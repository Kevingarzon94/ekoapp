android {
            defaultConfig {
                applicationId = "org.nativescript.appekoConductor"

                if (false) {
                    minSdkVersion = false
                }

                if (false) {
                    targetSdkVersion = false
                }
            }
        }